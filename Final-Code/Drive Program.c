#pragma config(Hubs,  S1, HTServo,  HTMotor,  HTMotor,  HTMotor)
#pragma config(Sensor, S1,     ,               sensorI2CMuxController)
#pragma config(Sensor, S2,     HTAC,           sensorI2CCustom)
#pragma config(Motor,  mtr_S1_C2_1,     chassisR,      tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C2_2,     chassisL,      tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C3_1,     scissorL,      tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C3_2,     scissorR,      tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C4_1,     bucketL,       tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C4_2,     bucketR,       tmotorTetrix, openLoop)
#pragma config(Servo,  srvo_S1_C1_4,    trailerServo,         tServoStandard)
#pragma config(Servo,  srvo_S1_C1_3,    gateServo,            tServoStandard)
#pragma config(Servo,  srvo_S1_C1_1,    bucketServoR,         tServoStandard)
#pragma config(Servo,  srvo_S1_C1_2,    bucketServoL,         tServoStandard)
#pragma config(Servo,  srvo_S1_C1_5,    servo5,               tServoNone)
#pragma config(Servo,  srvo_S1_C1_6,    servo6,               tServoNone)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//
/*
Hey ian, remeber that this is max code and disregard anything written.
One time a long time ago, betty and buddy went beihind a house to plant a garden. When they got there, buddy found a giant hole in the ground,
so naturally,  he hopped right in. Betty was a little more weary about it but she eventually went in. Suddenly
they were in another world. Filled with excitement and colors. Buddy started exploring all of the paths and all the new animals. Betty went to
the candy gardens and found a plethora of lolipops and candy trees. They were then devoured by a rock candy monster. es fin.
*/
#include "JoystickDriver.c"

//task liftPresets();
task driveBucketServos();

void driveChassis(bool t);
void driveBucket(bool t);
void driveLift(bool t);
void initServos();

static float fullFactor = 		0.781;
static float halfFactor = 		0.391;
static float scissorFactor = 	0.472;

bool servoToggle;

/*
 *	Driver 1: Tank drive w/ joysticks, A button fast, B button slow
 *  	LB Open hitch, RB Close hitch
 *
 *	Driver 2: Scissor lift presets (A -> B), bucket arms left stick w/ toggle stick click
 *		+ bucket servos on RT + LT, LB Open gate, RB close gate
 */

task main()
{
	servoToggle = false;
	initServos();
	nMotorEncoder[scissorL] = 0;
	nMotorEncoder[scissorR] = 0;
	bool chassisToggle = true;
	bool bucketToggle = true;
	bool liftToggle = false;

	srand(nSysTime);

	//StartTask(liftPresets);
	StartTask(driveBucketServos);

	while (true) {
		getJoystickSettings(joystick);

		//---------------------------Speed Toggles-------------------//
		if (joy1Btn(2)) {
			//wait1Msec(200);
			chassisToggle = true;
			PlayImmediateTone((rand() % (800-300)) + 300, 5);
		}

		if (joy1Btn(3)) {
			//wait1Msec(200);
			chassisToggle = false;
			PlayImmediateTone((rand() % (800-300)) + 300, 5);
		}

		if (joy2Btn(11)) {
			wait1Msec(200);
			bucketToggle = !bucketToggle;
			PlayImmediateTone((rand() % (800-300)) + 300, 5);
		}

		if (joy2Btn(12)) {
			wait1Msec(200);
			liftToggle = !liftToggle;
			PlayImmediateTone((rand() % (800-300)) + 300, 5);
		}

		//-------------------------Servos-----------------------------------//
		if (joy1Btn(5)) {     //open hitch
			servo[trailerServo] = 50;
		}

		if (joy1Btn(6)) {     //close hitch
			servo[trailerServo] = 5;
		}

		if (joy2Btn(5)) {     //open gate
			servo[gateServo] = 40;
		}

		if (joy2Btn(6)) {     //close gate
			servo[gateServo] = 200;
		}

		/*if (joy2Btn(8)) {      //bucket toggle, RT Button
			while (joy2Btn(8));
			if (servoToggle == true) {
				servo[bucketServoL] = 230;
				servo[bucketServoR] = 25;
				servoToggle = false;
				continue;
			} else {
				servo[bucketServoL] = 250;
				servo[bucketServoR] = 5;
				servoToggle = true;
				continue;
			}
		}*/

		driveChassis(chassisToggle);
		driveBucket(bucketToggle);
		driveLift(liftToggle); //maxcode//
	}
}

void driveChassis(bool t)
{
	if (t) {
		motor[chassisL] = joystick.joy1_y1 * fullFactor;
		motor[chassisR] = -joystick.joy1_y2 * fullFactor;
		} else {
		motor[chassisL] = joystick.joy1_y1 * scissorFactor;
		motor[chassisR] = -joystick.joy1_y2 * scissorFactor;
	}
}

void driveBucket(bool t)
{
	if (t) {
		motor[bucketL] = joystick.joy2_y1 * fullFactor;
		motor[bucketR] = -joystick.joy2_y1 * fullFactor;
		} else {
		motor[bucketL] = joystick.joy2_y1 * halfFactor;
		motor[bucketR] = -joystick.joy2_y1 * halfFactor;
	}
}

void driveLift(bool t)
{
	if (t) {
		motor[scissorL] = joystick.joy2_y2 * fullFactor;
		motor[scissorR] = -joystick.joy2_y2 * fullFactor;
		} else {
		motor[scissorL] = joystick.joy2_y2 * halfFactor;
		motor[scissorR] = -joystick.joy2_y2 * halfFactor;
	}
}

void initServos()
{
	servo[trailerServo] = 50;
	servo[gateServo] = 127;
	servo[bucketServoL] = 250;
	servo[bucketServoR] = 5;
}

task driveBucketServos()
{
	while(true) {
		if (joy2Btn(8) && ServoValue[bucketServoL] > 0 && ServoValue[bucketServoR] < 255) {
			servo[bucketServoL] = ServoValue[bucketServoL] - 2;
			servo[bucketServoR] = ServoValue[bucketServoR] + 2;
			wait1Msec(1);
		}

		if (joy2Btn(7) && ServoValue[bucketServoL] < 255 && ServoValue[bucketServoR] > 0) {
			servo[bucketServoL] = ServoValue[bucketServoL] + 2;
			servo[bucketServoR] = ServoValue[bucketServoR] - 2;
			wait1Msec(1);
		}
		wait1Msec(1);
	}
}

/*task liftPresets()
{
	while(true) {
		//PlayImmediateTone((rand() % (800-300)) + 300, 100);
		if (joy2Btn(2)) {
			PlayImmediateTone((rand() % (800-300)) + 300, 100);
			if (nMotorEncoder[scissorR] < 1000) {
				while (nMotorEncoder[scissorR] < 1000) {
					motor[scissorL] = 100;
					motor[scissorR] = -100;
					nxtDisplayCenteredBigTextLine(4, "%d", nMotorEncoder[scissorR]);
				}
				//return;
			} else if (nMotorEncoder[scissorR] > 1000) {
				while (nMotorEncoder[scissorR] > 1000) {
					motor[scissorL] = -20;
					motor[scissorR] = 20;
				}
				//return;
			} else {
				motor[scissorL] = 0;
				motor[scissorR] = 0;
			}
			motor[scissorL] = 0;
			motor[scissorR] = 0;
		}
		if (joy2Btn(1)) {
			PlayImmediateTone((rand() % (800-300)) + 300, 100);
			if (nMotorEncoder[scissorR] < 2000) {
				while (nMotorEncoder[scissorR] < 2000) {
					motor[scissorL] = 100;
					motor[scissorR] = -100;
					nxtDisplayCenteredBigTextLine(4, "%d", nMotorEncoder[scissorR]);
				}
				//return;
			} else if (nMotorEncoder[scissorR] > 2000) {
				while (nMotorEncoder[scissorR] > 2000) {
					motor[scissorL] = -20;
					motor[scissorR] = 20;
				}
				//return;
			} else {
				motor[scissorL] = 0;
				motor[scissorR] = 0;
			}
			motor[scissorL] = 0;
			motor[scissorR] = 0;
		}
		if (joy2Btn(4)) {
			PlayImmediateTone((rand() % (800-300)) + 300, 100);
			if (nMotorEncoder[scissorR] < 4000) {
				while (nMotorEncoder[scissorR] < 3000) {
					motor[scissorL] = 100;
					motor[scissorR] = -100;
					nxtDisplayCenteredBigTextLine(4, "%d", nMotorEncoder[scissorR]);
				}
				//return;
			} else if (nMotorEncoder[scissorR] > 3000) {
				while (nMotorEncoder[scissorR] > 3000) {
					motor[scissorL] = -20;
					motor[scissorR] = 20;
				}
				//return;
			} else {
				motor[scissorL] = 0;
				motor[scissorR] = 0;
			}
			motor[scissorL] = 0;
			motor[scissorR] = 0;
		}
		if (joy2Btn(3)) {
			PlayImmediateTone((rand() % (800-300)) + 300, 100);
			if (nMotorEncoder[scissorR] < 3000) {
				while (nMotorEncoder[scissorR] < 4000) {
					motor[scissorL] = 100;
					motor[scissorR] = -100;
					nxtDisplayCenteredBigTextLine(4, "%d", nMotorEncoder[scissorR]);
				}

			} else if (nMotorEncoder[scissorR] > 4000) {
				while (nMotorEncoder[scissorR] > 4000) {
					motor[scissorL] = -20;
					motor[scissorR] = 20;
				}

			} else {
				motor[scissorL] = 0;
				motor[scissorR] = 0;
			}
			motor[scissorL] = 0;
			motor[scissorR] = 0;
		}
		wait1Msec(10);
	}
}*/
